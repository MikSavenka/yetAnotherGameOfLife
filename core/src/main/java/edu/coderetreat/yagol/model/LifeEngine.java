package edu.coderetreat.yagol.model;

/**
 *
 * The life which dictate its own cruel rules (rule engine actually)
 * //todo make it singleton probably
 */
public class LifeEngine {

    Cell applyRules(Cell current) {

        return new Cell(0, 0, Cell.State.DEAD);     //todo

    }
}
